-# frozen_string_literal: true

%p 以下のレッスン内容に対するフィードバックを入力してください。

%strong= @lesson_feedback.lesson.summary

= simple_form_for(@lesson_feedback) do |f|
  = f.error_notification
  = f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present?

  .form-inputs
    = f.input :content
    = f.input :user_id, as: :hidden, input_html: { value: @lesson_feedback.user.id }
    = f.input :lesson_id, as: :hidden,  input_html: { value: @lesson_feedback.lesson.id }
  .form-actions
    = f.button :submit
