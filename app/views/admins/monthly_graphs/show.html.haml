%h1 #{@teacher.name}さんの予約率

.table-responsive
  %table.table.table-bordered
    %thead
      %th 月
      %th 予約数
      %th レッスン数

    %tbody
      - @teachers_stats.each do |stat|
        %tr
          %td
            = stat[:month]
          %td
            = stat[:reserve_count]
          %td
            = stat[:lesson_count]
